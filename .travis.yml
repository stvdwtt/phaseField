sudo: required
dist: trusty
language: cpp

services: 
  - docker
  
before_install:
  - docker pull dealii/dealii:v8.4.2-gcc-mpi-fulldepsmanual-release
  - docker run -it dealii/dealii:v8.4.2-gcc-mpi-fulldepsmanual-release /bin/bash
  

script: bash test_ci.sh
